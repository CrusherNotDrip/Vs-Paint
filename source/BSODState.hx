/*
package;

import flixel.text.FlxText;
import flixel.FlxG;
import flixel.util.FlxColor;

class BSODState extends MusicBeatState {
    public function new(smiley, bsodtext) {
        var smileyText:FlxText = new FlxText(FlxG.width + 20, FlxG.height + 30, 0, (smiley ? smiley : ':('), 40);
		smileyText.scrollFactor.set();
		smileyText.setFormat("VCR OSD Mono", 16, FlxColor.WHITE, LEFT, FlxTextBorderStyle.OUTLINE, FlxColor.BLACK);
		add(smileyText);
    }
}
*/
//Hayfi thought it would be really dumb to make a cheap bsod (blue screen of death) state, of course it wont ACTUALLY bsod your pc.